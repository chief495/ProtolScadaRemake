<Window xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        x:Class="ProtolScadaRemake.DialogElementMixer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Height="250"
        Width="520"
        Background="#FFCCC9C9"
        FontFamily="Arial"
        Icon="Images/ProtolLogo.ico"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <GroupBox Header="Режим работы" Grid.Row="0" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" Margin="10,4">
                <RadioButton x:Name="RBAuto" Content="Автоматический" Margin="0,0,20,0" Checked="RBAuto_Checked"/>
                <RadioButton x:Name="RBManual" Content="Ручной" Checked="RBManual_Checked"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="Время работы, сек" Grid.Row="1" Margin="0,0,0,10">
            <StackPanel Margin="10,4">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                    <TextBlock Text="Запуск:" Width="120" VerticalAlignment="Center"/>
                    <Controls:NumericUpDown x:Name="StartTimeNumeric" Width="130" Minimum="0" Maximum="36000" StringFormat="##0" HideUpDownButtons="True" ValueChanged="StartTimeNumeric_ValueChanged"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Остановка:" Width="120" VerticalAlignment="Center"/>
                    <Controls:NumericUpDown x:Name="StopTimeNumeric" Width="130" Minimum="0" Maximum="36000" StringFormat="##0" HideUpDownButtons="True" ValueChanged="StopTimeNumeric_ValueChanged"/>
                </StackPanel>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="Fault" Grid.Row="2">
            <StackPanel Orientation="Horizontal" Margin="10,6">
                <TextBlock Text="Идентификатор:" Width="120" VerticalAlignment="Center"/>
                <TextBox x:Name="FaultIdText" Width="340" IsReadOnly="True"/>
            </StackPanel>
        </GroupBox>
    </Grid>
</Window>
