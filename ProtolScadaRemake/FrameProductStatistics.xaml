<UserControl x:Class="ProtolScadaRemake.FrameProductStatistics"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="600">

    <UserControl.Resources>
        <!-- ControlTemplate для закругленных кнопок -->
        <ControlTemplate x:Key="RoundedButtonTemplate" TargetType="Button">
            <Border Background="{TemplateBinding Background}" 
                    BorderBrush="{TemplateBinding BorderBrush}" 
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="8"
                    Padding="{TemplateBinding Padding}">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
        </ControlTemplate>

        <!-- Стиль для кнопок сброса -->
        <Style x:Key="ResetButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#FF00FF00"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template" Value="{StaticResource RoundedButtonTemplate}"/>
            <Setter Property="Background" Value="#FF1E5945"/>
            <Setter Property="Margin" Value="10,5,0,0"/>
        </Style>

        <!-- ControlTemplate для закругленных полей ввода -->
        <ControlTemplate x:Key="RoundedTextBoxTemplate" TargetType="TextBox">
            <Border Background="{TemplateBinding Background}" 
                    BorderBrush="{TemplateBinding BorderBrush}" 
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="5">
                <ScrollViewer x:Name="PART_ContentHost" 
                              Margin="{TemplateBinding Padding}"
                              Background="Transparent"/>
            </Border>
        </ControlTemplate>

        <!-- Стиль для полей ввода -->
        <Style x:Key="CounterTextBoxStyle" TargetType="TextBox">
            <Setter Property="Width" Value="180"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="Background" Value="#FFE0E0E0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#FF666666"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template" Value="{StaticResource RoundedTextBoxTemplate}"/>
            <Setter Property="Margin" Value="5,5,0,0"/>
        </Style>

        <!-- Стиль для статусной панели -->
        <Style x:Key="StatusPanelStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="2"/>
        </Style>

        <!-- Стиль для меток -->
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="TextAlignment" Value="Right"/>
            <Setter Property="Margin" Value="0,5,5,0"/>
        </Style>

        <!-- Стиль для единиц измерения -->
        <Style x:Key="UnitStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="5,5,0,0"/>
        </Style>
    </UserControl.Resources>

    <!-- ОСНОВНОЙ КОНТЕЙНЕР С ПРОЗРАЧНЫМ ФОНОМ -->
    <Grid Background="Transparent">

        <!-- Внешняя рамка толщиной 3 -->
        <Border Background="Transparent" 
                BorderBrush="#FF666666" 
                BorderThickness="3" 
                CornerRadius="10"
                Margin="10">

            <!-- Внутренняя рамка -->
            <Border Background="Transparent" 
                    BorderBrush="#FF555555" 
                    BorderThickness="1" 
                    CornerRadius="8"
                    Margin="5"
                    Padding="20">

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Заголовок -->
                    <Border Grid.Row="0" 
                            Background="Transparent" 
                            BorderBrush="#FF666666" 
                            BorderThickness="0,0,0,2"
                            CornerRadius="5,5,0,0"
                            Padding="0,0,0,10"
                            Margin="0,0,0,15">
                        <TextBlock Text="СТАТИСТИКА ПРОИЗВОДСТВА" 
                                   FontSize="22" 
                                   FontWeight="Bold" 
                                   Foreground="#FFFFFF"
                                   HorizontalAlignment="Center"/>
                    </Border>

                    <!-- Содержимое -->
                    <Grid Grid.Row="1" Margin="0,0,0,15">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="20"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Первая строка: Продукция за смену -->
                        <Grid Grid.Row="0" Margin="0,0,0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Метка (выровнена вправо) -->
                            <TextBlock Grid.Column="0" 
                                       Text="Продукция за смену"
                                       Style="{StaticResource LabelStyle}"/>

                            <!-- Поле ввода -->
                            <TextBox Grid.Column="1" 
                                     x:Name="CounterEdit" 
                                     Style="{StaticResource CounterTextBoxStyle}"/>

                            <!-- Единицы измерения (кг) -->
                            <TextBlock Grid.Column="2" 
                                       Text="кг"
                                       Style="{StaticResource UnitStyle}"/>
                        </Grid>

                        <!-- Кнопка сброса счетчика смены -->
                        <Button Grid.Row="1" 
                                x:Name="ResetCounterButton"
                                Content="СБРОСИТЬ СЧЕТЧИК СМЕНЫ"
                                Style="{StaticResource ResetButtonStyle}"
                                HorizontalAlignment="Left"
                                Click="ResetCounterButton_Click"
                                Width="202"
                                Margin="185,5,0,20" Grid.RowSpan="2"/>

                        <!-- Вторая строка: Вся произведенная продукция -->
                        <Grid Grid.Row="3" Margin="0,0,0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Метка (выровнена вправо) -->
                            <TextBlock Grid.Column="0" 
                                       Text="Вся произведенная продукция"
                                       Style="{StaticResource LabelStyle}"/>

                            <!-- Поле ввода -->
                            <TextBox Grid.Column="1" 
                                     x:Name="TotalCounterEdit" 
                                     Style="{StaticResource CounterTextBoxStyle}"/>

                            <!-- Единицы измерения (кг) -->
                            <TextBlock Grid.Column="2" 
                                       Text="кг"
                                       Style="{StaticResource UnitStyle}"/>
                        </Grid>

                        <!-- Кнопка сброса общего счетчика -->
                        <Button Grid.Row="4" 
                                x:Name="ResetTotalCounterButton"
                                Content="СБРОСИТЬ ОБЩИЙ СЧЕТЧИК"
                                Style="{StaticResource ResetButtonStyle}"
                                HorizontalAlignment="Left"
                                Click="ResetTotalCounterButton_Click"
                                Width="202"
                                Margin="185,5,0,0"/>
                    </Grid>

                    <!-- Статус -->
                    <Border Grid.Row="2" 
                            x:Name="StatusBorder"
                            Style="{StaticResource StatusPanelStyle}"
                            Background="#FF007ACC"
                            BorderBrush="#FF666666"
                            Margin="0,15,0,0">
                        <TextBlock x:Name="StatusText"
                                   Text="Статус: Загрузка..."
                                   Foreground="White"
                                   HorizontalAlignment="Center"
                                   FontSize="14"
                                   FontWeight="Bold"/>
                    </Border>
                </Grid>
            </Border>
        </Border>
    </Grid>
</UserControl>