<UserControl x:Class="ProtolScadaRemake.Views.FrameLog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Панель фильтров -->
        <Border Grid.Row="0" 
                BorderBrush="LightGray" BorderThickness="0,0,0,1" 
                Padding="10" Margin="10,10,10,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Левый блок: Период -->
                <StackPanel Grid.Column="0" Grid.Row="0" Orientation="Horizontal">
                    <!-- Период -->
                    <StackPanel Orientation="Vertical" Margin="0,0,20,0">
                        <TextBlock Text="Период" FontWeight="Bold" Margin="0,0,0,5"/>
                        <ComboBox x:Name="cmbPeriod" Width="150" SelectedIndex="0" SelectionChanged="cmbPeriod_SelectionChanged">
                            <ComboBoxItem Content="За сутки"/>
                            <ComboBoxItem Content="За неделю"/>
                            <ComboBoxItem Content="За месяц"/>
                            <ComboBoxItem Content="Произвольный период"/>
                        </ComboBox>
                    </StackPanel>

                    <!-- Интервал -->
                    <StackPanel x:Name="DateIntervalPanel" Orientation="Vertical" Margin="0,0,20,0">
                        <TextBlock Text="Интервал" FontWeight="Bold" Margin="0,0,0,5"/>
                        <StackPanel Orientation="Horizontal">
                            <DatePicker x:Name="dpFrom" Width="120" SelectedDateFormat="Short" IsEnabled="False"/>
                            <TextBlock Text="по:" VerticalAlignment="Center" Margin="5,0,5,0"/>
                            <DatePicker x:Name="dpTo" Width="120" SelectedDateFormat="Short" IsEnabled="False"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>

                <!-- Центральный блок: Тип записи -->
                <StackPanel Grid.Column="0" Grid.Row="1" Orientation="Vertical" Margin="0,10,0,0">
                    <TextBlock Text="Тип записи" FontWeight="Bold" Margin="0,0,0,5"/>
                    <WrapPanel>
                        <CheckBox x:Name="chkAlarm" Content="Авария" Margin="5,2" IsChecked="True"/>
                        <CheckBox x:Name="chkWarning" Content="Предупреждение" Margin="5,2" IsChecked="True"/>
                        <CheckBox x:Name="chkError" Content="Отказ" Margin="5,2" IsChecked="True"/>
                        <CheckBox x:Name="chkFault" Content="Сбой" Margin="5,2" IsChecked="True"/>
                        <CheckBox x:Name="chkUser" Content="Пользователь" Margin="5,2" IsChecked="True"/>
                        <CheckBox x:Name="chkSystem" Content="Система" Margin="5,2" IsChecked="True"/>
                    </WrapPanel>
                </StackPanel>

                <!-- Кнопки справа -->
                <StackPanel Grid.Column="1" Grid.Row="0" Grid.RowSpan="2" 
                            HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button x:Name="btnRefresh" 
                            Content="Обновить" 
                            Width="100" 
                            Height="30" 
                            Margin="0,0,0,10"
                            Background="#FF007ACC"
                            Foreground="White"
                            Click="btnRefresh_Click"
                            Cursor="Hand"/>
                    <Button x:Name="btnExport" 
                            Content="Экспорт" 
                            Width="100" 
                            Height="30"
                            Background="#FF007ACC"
                            Foreground="White"
                            Click="btnExport_Click"
                            Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Заголовок таблицы -->
        <Border Grid.Row="1" 
                Background="#F5F5F5" Padding="5" Margin="10,5,10,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Время" FontWeight="Bold" HorizontalAlignment="Center"/>
                <TextBlock Grid.Column="1" Text="Группа" FontWeight="Bold" HorizontalAlignment="Center"/>
                <TextBlock Grid.Column="2" Text="Текст" FontWeight="Bold" HorizontalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Список событий -->
        <ListView x:Name="lvLog" Grid.Row="2" 
                  BorderThickness="0" Margin="10,0,10,10"
                  HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Setter Property="BorderBrush" Value="LightGray"/>
                    <Setter Property="Padding" Value="5"/>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.View>
                <GridView>
                    <!-- Время -->
                    <GridViewColumn Width="150">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Time, StringFormat='dd.MM.yyyy HH:mm:ss'}" 
                                           FontSize="11" HorizontalAlignment="Center"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Группа -->
                    <GridViewColumn Width="150">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding GroupName}" 
                                           FontSize="11" HorizontalAlignment="Center"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Текст -->
                    <GridViewColumn Width="Auto">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Text}" 
                                           FontSize="11" TextWrapping="Wrap"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>