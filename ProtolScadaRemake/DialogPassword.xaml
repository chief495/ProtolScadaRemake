<Window x:Class="ProtolScadaRemake.DialogPassword"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Ввод пароля"
        Width="400" 
        Height="233"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize"
        Topmost="True"
        KeyDown="DialogPassword_KeyDown"
        Loaded="DialogPassword_Loaded">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" 
                   Text="Введите пароль для получения прав доступа"
                   FontSize="14"
                   FontWeight="Bold"
                   Margin="0,0,0,20"
                   HorizontalAlignment="Center"
                   TextWrapping="Wrap"/>

        <!-- Поле ввода пароля -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="Пароль:" Margin="0,0,0,5"/>
            <PasswordBox x:Name="pass" 
                         PasswordChar="•"
                         Height="30"
                         FontSize="14"
                         Padding="5,0"
                         KeyDown="pass_KeyDown"/>
        </StackPanel>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center"
                    Margin="0,10,0,0">

            <Button x:Name="OKButton" 
                    Content="OK" 
                    Width="80" 
                    Height="30"
                    Margin="0,0,10,0"
                    Background="#267ABB"
                    Foreground="White"
                    FontWeight="Bold"
                    Click="OK_Click"/>

            <Button x:Name="CancelButton" 
                    Content="Отмена" 
                    Width="80" 
                    Height="30"
                    Background="#7B001C"
                    Foreground="White"
                    FontWeight="Bold"
                    Click="CancelButton_Click"/>
        </StackPanel>

        <!-- Сообщение об ошибке (изначально скрыто) -->
        <TextBlock x:Name="ErrorTextBlock" 
                   Grid.Row="3"
                   Text="Неверный пароль!"
                   Foreground="Red"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Bottom"
                   Visibility="Collapsed"
                   Margin="0,10,0,0"/>
    </Grid>
</Window>